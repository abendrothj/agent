# internal/api — generated protobuf stubs and .proto source files
#
# Import strategy used by each service's grpc_server.py:
#
#   import sys
#   from pathlib import Path
#   _PROTO_DIR = Path(__file__).resolve().parent.parent.parent / "internal" / "api"
#   if str(_PROTO_DIR) not in sys.path:
#       sys.path.insert(0, str(_PROTO_DIR))
#   import vault_pb2, vault_pb2_grpc          # (or shadow_, sandbox_, etc.)
#
# Run  scripts/gen_protos.sh  from the project root to regenerate stubs
# after modifying any .proto file.
