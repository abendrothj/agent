# Python dependencies for Vault service
psycopg[binary]>=3.1.14
redis>=5.0.0
grpcio>=1.60.0
grpcio-tools>=1.60.0
python-dotenv>=1.0.0
pydantic>=2.0.0
prometheus-client>=0.19.0

# LangGraph — state machine control flow
langgraph>=0.2.0
langgraph-checkpoint-postgres>=0.1.0
langchain-core>=0.2.0

# Neo4j — real-time graph writes to memory_store container
neo4j>=5.0.0

# Git autonomy — autonomous repo discovery, contribution, PR submission
gitpython>=3.1.40
httpx>=0.27.0

# MCP — Model Context Protocol (peripheral nervous system / sensory organs)
# Provides: structured sensory input to LangGraph before risk classification.
# mcp         = the protocol SDK (client + server framework)
# mcp-server-git = Python git server: git_log, git_diff, git_status tools
# Both are free. No API cost — all transport is local stdio.
mcp>=1.0.0
mcp-server-git>=0.1.0
