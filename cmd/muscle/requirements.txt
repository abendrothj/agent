# Teammate Muscle Service Dependencies
# Install with: pip install -r requirements.txt

# gRPC & Protobuf
grpcio>=1.60.0
grpcio-tools>=1.60.0

# PyTorch (CPU will fallback, CUDA accelerated if available)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Hugging Face Transformers
transformers>=4.35.0
accelerate>=0.24.0
bitsandbytes>=0.41.0  # For 8-bit quantization support

# HTTP (async)
httpx>=0.25.0

# Configuration & Logging
python-dotenv>=1.0.0
loguru>=0.7.0

# Utilities
pydantic>=2.0.0
cryptography>=41.0.0

# Windows-specific (activity detection)
pywin32>=306  # For GetLastInputInfo on Windows
